{
  "name": "mitotracker-root",
  "private": true,
  "dependencies": {
    "next": "15.5.6",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "scripts": {
    "postinstall": "npm ci --prefix ./frontend || npm install --prefix ./frontend",
    "dev": "npm --prefix ./frontend run dev",
    "build": "npm ci --prefix ./frontend || npm install --prefix ./frontend && npm --prefix ./frontend run build",
    "start": "npm --prefix ./frontend run start"
  },
  "devDependencies": {}
}